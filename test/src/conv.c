// #include "lex.h"
#include "common.h"
#include <stdbool.h>
#include <string.h>

// absolutely not touched stuff (8 lines only)

int switchARooOrSmnthIdk(Tok* line) {
  if (strcmp(line->token, "sched")) {

  } else if (strcmp(line->token, "event")) {

  } else if (strcmp(line->token), "todo") {
  }
  return 1;
}
